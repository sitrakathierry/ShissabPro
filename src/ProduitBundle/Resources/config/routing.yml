produit_homepage:
    path:     /produit/index
    defaults: { _controller: ProduitBundle:Default:index }

produit_add:
    path:     /produit/add
    defaults: { _controller: ProduitBundle:Default:add }

produit_save:
    path:     /produit/save
    defaults: { _controller: ProduitBundle:Default:save }
    options:
        expose: true
    methods: [POST]

produit_consultation:
    path:     /produit/consultation
    defaults: { _controller: ProduitBundle:Default:consultation }

produit_list:
    path:     /produit/list
    defaults: { _controller: ProduitBundle:Default:list }
    options:
        expose: true
    methods: [POST]